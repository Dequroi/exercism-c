#ifndef ELIUDS_EGGS_H
#define ELIUDS_EGGS_H

// Header & C are blank, check testing

// Use unsigned int to represent the number of eggs, as it cannot be negative
unsigned int egg_count(unsigned int eggs);

#endif
